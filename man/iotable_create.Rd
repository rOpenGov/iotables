% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iot_create.R
\name{iotable_create}
\alias{iotable_create}
\title{Create a stacked symmetric input–output table (SIOT)}
\usage{
iotable_create(q1, q2, q3, q1_id, final_use_id, primary_id, id_col = "prod_na")
}
\arguments{
\item{q1}{Numeric \verb{n × n} matrix (Quadrant 1). Row/col names are ids.}

\item{q2}{Numeric \verb{n × k} matrix (Quadrant 2). Row names = \code{q1} ids.}

\item{q3}{Numeric \verb{m × n} matrix (Quadrant 3). Col names = \code{q1} ids.}

\item{q1_id}{Character vector of industries/products (length = n).}

\item{final_use_id}{Character vector of final-demand categories (len = k).}

\item{primary_id}{Character vector of primary inputs (length = m).}

\item{id_col}{Name of the identifier column in the output tibble.}
}
\value{
A tibble with rows = industries + primary inputs,
cols = \code{id_col}, industries, final demand. Quadrant 4 is \code{NA_real_}.
}
\description{
Build a SIOT with the Eurostat/Beutel layout:
\itemize{
\item Quadrant 1: intermediate uses (\strong{Z}), industries × industries.
\item Quadrant 2: final uses (\strong{Y}), industries × final-demand categories.
\item Quadrant 3: primary inputs (\strong{V}), primary-input rows × industries.
\item Quadrant 4: empty block (primary-input rows × final-demand cols),
filled with \code{NA_real_}.
}
}
\examples{
ids <- c("p1","p2")
final_ids <- c("hfce","gfcf")
prim_ids <- c("comp_lab","os_mi")

q1 <- matrix(c(10,2, 3,15), 2, byrow = TRUE,
             dimnames = list(ids, ids))
q2 <- matrix(c(5,7, 8,9), 2,
             dimnames = list(ids, final_ids))
q3 <- matrix(c(12,14, 6,11), 2,
             byrow = TRUE,
             dimnames = list(prim_ids, ids))

siot <- iotable_create(q1,q2,q3,
                       q1_id=ids,
                       final_use_id=final_ids,
                       primary_id=prim_ids)
siot

}
