% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrant_separator_find.R
\name{quadrant_separator_find}
\alias{quadrant_separator_find}
\title{Locate the end of the inter-industry block (Quadrant I)}
\usage{
quadrant_separator_find(data_table, include_total = FALSE)
}
\arguments{
\item{data_table}{A wide SIOT/use table where the first column is a key
(product/industry label) and subsequent columns are numeric flows.}

\item{include_total}{Logical. If \code{TRUE}, and a \code{"total"}/\code{"cpa_total"} column
is present, return its \strong{own} index; if \code{FALSE} (default), return the
\strong{last inter-industry} column (i.e. one before total).}
}
\value{
An \strong{integer} column index (or an integer vector if multiple matches
exist). If no marker is found, returns \code{2L} with a warning.
}
\description{
Detects the \strong{column index} of the last inter-industry (Quadrant I) column
in a wide symmetric input–output (SIOT) or use table. This separator is used
to split the inter-industry block from \strong{final uses} (Quadrant III) and other
columns before computing coefficients and inverses.
}
\details{
The function applies a small set of \strong{case-insensitive heuristics} over the
column names (first column is assumed to be the key/label column):
\enumerate{
\item If a column named \code{"total"} or \code{"cpa_total"} exists, its index is used
as the boundary. If \code{include_total = FALSE} (default), the \strong{column
before} total is returned.
\item Otherwise, if a column named \code{"households"} or \code{"p13_s14"} exists,
the separator is placed \strong{immediately before} that column.
\item Otherwise, if a column named one of \code{"cpa_u"}, \code{"cpa_t"}, or \code{"cpa_s96"}
exists, that column index is used (these are common last CPA aggregates).
\item Otherwise, if \code{"other_services_group"} exists, its index is used.
}

If none of the above markers are found, the function returns \strong{2} and emits
a warning. Returning 2 (i.e. first numeric column only) avoids aborting
downstream pipelines but indicates that column metadata should be checked.

Notes:
\itemize{
\item Matching is \strong{case-insensitive}.
\item If \strong{multiple} potential columns match (e.g. both \code{"total"} and
\code{"cpa_total"}), all matching indices are returned. Callers should ensure
uniqueness upstream if a single index is required.
\item Set \code{include_total = TRUE} when you explicitly want the index of the total
column itself (e.g. for consistency checks).
}
}
\examples{
# Minimal SIOT (wide):
small_io <- data.frame(
  prod_na = c("CPA_A", "CPA_B", "CPA_C", "output"),
  CPA_A   = c(10, 4, 1, 35),
  CPA_B   = c(2, 8, 5, 30),
  CPA_C   = c(3, 2, 6, 30),
  total   = c(20, 16, 18, NA_real_)
)

# End of Quadrant I (before 'total'):
quadrant_separator_find(small_io)
# include_total = TRUE returns the index of the 'total' column:
quadrant_separator_find(small_io, include_total = TRUE)

}
\seealso{
\code{\link{coefficient_matrix_create}},
\code{\link{input_coefficient_matrix_create}}
}
\keyword{internal}
